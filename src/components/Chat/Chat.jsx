import React from 'react';

const Chat = ({ roomId }) => {
  return (
    <div>
      {/* Здесь будет ваш компонент для чата */}
      <h2>Chat Room {roomId}</h2>
    </div>
  );
};

export default Chat;